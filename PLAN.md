# خطة قصيرة

الفكرة: مساعد سيرة ذاتية محلي يقارن نص السيرة بوصف وظيفي ويعرض درجة توافق والكلمات المتطابقة والمفقودة. يعمل بالكامل داخل المتصفح من دون رفع البيانات أو الحاجة إلى خدمة خارجية.

1. بناء واجهة عربية بسيطة مع مثال جاهز والتحقق من المدخلات.
2. تنفيذ تحليل كلمات حتمي يمكن اختباره، ثم ربطه بالواجهة.
3. تشغيل الاختبارات وتجربة الواجهة، ثم تكليف وكيل QA بمراجعة مستقلة.
4. إصلاح أي ملاحظة، وإعادة الاختبار، وتوثيق التشغيل وإرفاق لقطتي شاشة وZIP.

## عقد الإصدار الأول

- المدخلات: نص السيرة الذاتية ونص الوصف الوظيفي، وكلاهما مطلوب.
- المخرجات: درجة من 0 إلى 100، كلمات متطابقة، كلمات مفقودة، وتوصية قصيرة.
- واجهة المحلل: تصدير ES module باسم `analyzeCv(cvText, jobDescription)` يعيد `{ score, matchedTerms, missingTerms, recommendation }`، ويرمي `TypeError` برسالة عربية ثابتة للمدخل غير الصالح.
- الأخطاء: رسالة عربية واضحة عند غياب أحد النصين، دون تعطل الصفحة.
- الخصوصية: لا شبكة، لا تخزين محلي، ولا سجلات تحتوي نصوص المستخدم.
- ملكية الملفات: وكيل `frontend` يملك `index.html` و`styles.css` و`app.js` و`tests/`؛ القائد يملك الخطة وREADME وملفات الأدلة ولقطات الشاشة والحزمة؛ وكيل `qa` يراجع فقط.

## خطة نسخة العرض المحسنة

1. تحسين المطابقة العربية والعبارات المهنية الشائعة مع الحفاظ على تحليل محلي حتمي بلا مكتبات.
2. توضيح مستوى النتيجة وأساسها، وإظهار أهم الأولويات فقط بدل إغراق المستخدم بقائمة طويلة.
3. إضافة إعادة ضبط سهلة وتحسين العرض المتجاوب وإتاحة الاستخدام.
4. إعادة المراجعة المستقلة والتصوير والحزم، ثم تحديث GitHub ونشر نسخة تجريبية عامة.

### عقد التحليل المحسن

- يبقى التصدير `analyzeCv(cvText, jobDescription)` متوافقًا مع الإصدار السابق.
- يضيف الناتج `level` و`summary` و`topSuggestions` إلى `score` و`matchedTerms` و`missingTerms` و`recommendation`.
- جميع النصوص المستخرجة تأتي من الوصف الوظيفي فقط، ولا تظهر بيانات اتصال أو نص خاص بالسيرة في النتيجة.
- لا شبكة ولا تخزين ولا اعتماد خارجي؛ الاختبارات تستخدم Node المدمج فقط.
